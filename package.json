{
  "name": "sentinel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Autonomous BNB-Chain trading agent: reads the chain, pays its own way via x402, learns from every trade.",
  "scripts": {
    "dev": "tsx src/agent.ts",
    "tracer": "tsx src/agent.ts --once",
    "backtest": "tsx src/backtest/run.ts",
    "signals": "tsx src/signals/inspect.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "constitution": "tsx src/identity/commit.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "dotenv": "16.4.7",
    "openai": "4.77.0",
    "viem": "2.21.55",
    "zod": "3.24.1"
  },
  "devDependencies": {
    "@types/node": "22.10.5",
    "tsx": "4.19.2",
    "typescript": "5.7.2",
    "vitest": "2.1.8"
  },
  "comment_deps": "x402 stack (@x402/axios @x402/evm) added in Phase 2 once the spike confirms the exact 2.14.0 export symbols. Versions pinned per SERVICES-AND-APIS.md."
}
